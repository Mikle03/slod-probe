# AI Usage Disclosure

AI coding assistants were used to support the literature-review preparation, prototype implementation, verification, and documentation of this project. The author reviewed the outputs and remains responsible for the experimental design, interpretation, correctness, and submitted work.

The three major prompts below are reproduced verbatim, without rewriting or correction. The first prompt was used to develop the conceptual foundation for the literature summary. The second prompt specified the implementation of the SLoD probing prototype. The third prompt was used to structure the technical report.

## Major prompt 1 — literature summary (verbatim)

```text
Structure my insights into the literature review. Don’t add anything apart from the thoughts I write to you now.
So, since the first deliverable is a Literature Summary, which covers subtopics such as probing methodology and what probing classifiers can and cannot tell us about representations, hierarchical structure in embeddings and what is known about how abstract and concrete concepts are encoded, weak supervision approaches and how document structure can replace manual annotation, and applicability to SLoD, meaning which insights are most relevant to detecting abstraction level in scientific text, we first need to understand that the literature mentioned above gives us the introductory foundation for this whole topic. The papers on probing and representation analysis, such as Tenney, Das, and Pavlick’s “BERT Rediscovers the Classical NLP Pipeline” and Belinkov’s “Probing Classifiers: Promises, Shortcomings, and Advances,” together with the papers on hierarchical and hyperbolic representations, such as Nickel and Kiela’s “Poincaré Embeddings for Learning Hierarchical Representations” and Vilnis and McCallum’s “Word Representations via Gaussian Embedding,” and also the papers on scientific text and weak supervision, such as Lo et al.’s “S2ORC: The Semantic Scholar Open Research Corpus” and Ratner et al.’s “Snorkel: Rapid Training Data Creation with Weak Supervision,” all serve as background for understanding probing classifiers as diagnostic tools. These tools can be used to test whether a specific property can be predicted from a model’s internal representations of the input. This is a useful technique because it gives us a way to gain insights into large language models and pretrained encoders, and these papers explain how we can use this tool, how we can apply it, and how we should analyze the results. Going deeper into the first subtopic, probing methodology, it is important to explain how the whole mechanism of probing classifiers works. There is some pretrained model, and we want to understand whether it can give us a certain insight about a particular field or a particular aspect of textual information. As we can see from the literature, researchers used this kind of method, for example, to understand whether a model can distinguish different parts of speech or encode morphological, syntactic, semantic, and other linguistic properties. In our case, the task is to understand whether we can detect abstraction level in scientific text based on frozen embeddings. Continuing with the functionality, the process works in the following way. We have a certain input, and we want to classify it in order to understand whether we can decode particular information from the model’s representation. So we take the input and produce frozen embeddings through an encoder, or through a pretrained model, in order to create a stable representation for this input text. Then a separate classifier is trained on top of these representations. After we receive the internal representation of the input and train the classifier, it is important that the classifier should not be too complex. Otherwise, it may give higher accuracy, but at the same time it may simply memorize certain patterns in the text. Therefore, in order to reveal whether a particular property is decodable, we use simple classifiers, such as a linear probing classifier. So, after we get these frozen embeddings, we use them as input for a linear probing classifier, and the classifier tries to classify these embeddings into the categories connected to the property that we want to investigate. In our project, this property is the semantic level of detail. In other words, we want to know whether we can determine the abstraction level of the text by classifying a given text span into macro, meso, or micro levels of abstraction. Manually, we can often understand which type a text belongs to based on certain observations, such as what vocabulary is used, how general or detailed the text is, and whether it contains broad claims or technical details. In our case, if the probing classifier performs well, this suggests that the target property is extractable from the representation. The most careful interpretation is that the property is decodable from the embedding space, but not that the model definitely understands and uses this particular property in its output generation. This distinction is very important because probing classifiers show whether information can be recovered from representations, but they do not directly prove that the original model uses this information in its own reasoning or generation. At the same time, there are certain risks. High results may actually correlate with other properties of the text, not with semantic abstraction itself. Therefore, the literature says that it is important to conduct additional control analysis. For linear probes, as mentioned above, we usually use simple models such as logistic regression or linear SVM. Also, probing results should be compared against baselines. For example, we can use a majority-class baseline because it shows what performance can be achieved without using embeddings meaningfully. In this project, we will also use a length-control experiment and cross-domain evaluation, so that the model does not simply rely on certain words that appear more often in one context or one part of the text. Moving to the second question, hierarchical structure in embeddings, we can say that research shows representations can encode more than simple semantic similarity. They can also include hierarchy, asymmetric relations, specificity, uncertainty, and other more complex structures. This is relevant for our project because SLoD is naturally organized at different abstraction levels. It is a hierarchical way of presenting information. The more detailed the text is, the more it moves toward lower-level, specific information, while higher-level representations may be summaries, broad claims, or more general descriptions of the same content. At the macro level, text may contain broad claims, research contributions, conclusions, and general motivation. The more detailed the text becomes, the more concrete details appear, such as numbers, methodological steps, model details, experimental settings, metrics, and so on. Nickel and Kiela’s work on Poincaré embeddings explains that hyperbolic space is well suited for representing tree-like or hierarchical structures. However, we are not going to use hyperbolic embeddings in our project. In this sense, SLoD can be treated as a practical form of semantic hierarchy in scientific writing. Another paper by Vilnis and McCallum discusses Gaussian embeddings, which are another format of representation and help reveal other aspects and properties of embeddings, such as uncertainty and specificity. This is relevant because abstract and concrete concepts may differ in how broad or specific they are. Therefore, the SLoD probe tests whether this kind of abstraction-level information is already recoverable from frozen language model embeddings. Then, speaking about weak supervision, we first need to define weak supervision. It is a way to generate training labels using particular rules or signals instead of manual annotation. If we do not have large manually labeled datasets to train the model, then we need to get this data from somewhere in order to train the classifier and tell it what each example represents. Manual labeling can be very expensive because it requires human labor. Therefore, Ratner and others introduced Snorkel as a framework for creating labeled data through labeling functions. Labeling functions are programmatic rules that assign labels to data points. These rules are not perfect, but they can still create useful training data. They are not perfect because research papers do not always use the same table of contents or the same section names. Because of this, it is difficult to rely only on programmed keywords or section labels for labeling data. In our case, we are interested in different levels of text abstraction. In this weak supervision framework, we can use criteria such as the table of contents, section names, certain words that appear more often in abstract texts than in detailed texts, and also certain metric or technical parameters that are expressed through methods, experiments, and detailed results. These sections often contain operational, technical, and numerical details. For example, in our case, title, abstract, introduction, and conclusion sections can be assigned the macro label. Section lead paragraphs can be assigned the meso label because they often introduce the purpose or content of the section. Methods, experiments, and detailed results paragraphs can be assigned the micro label because they usually contain operational, technical, and numerical details. In this way, we can apply section-based rule functions in order to obtain data for training. The Semantic Scholar Open Research Corpus introduces a large corpus of scientific papers with metadata, abstracts, references, and structured full text. Therefore, it is useful for our project because it preserves scientific document structure. This helps us extract text spans and label them automatically based on their position in the paper. Finally, we come to the applicability to SLoD. We already started discussing this earlier, but here we should add the information that does not simply repeat the previous points. The SLoD task combines three research ideas: probing classifiers, hierarchical representations, and weak supervision from structured scientific text. Probing methodology provides the experimental framework, meaning it tells us how to build the experiment. Weak supervision gives us the labeling strategy, meaning that it helps us obtain training data and test data. Hierarchical representation research gives us the conceptual background for thinking about abstraction level as something that may be reflected in embeddings. In our design, we will keep the encoder frozen, meaning it is not trained on the same data for the SLoD task. The encoder only produces embeddings. The probing classifier is trained on the training embeddings and then tested on unseen test embeddings. We will also consider several main risks. The strongest result would be high performance in three conditions: in-domain, cross-domain, and length-controlled. In-domain performance shows whether the signal is recoverable or extractable within one scientific domain. Cross-domain performance tests whether the signal generalizes beyond domain-specific vocabulary. Length-controlled performance tests whether the probe is using semantic abstraction rather than simply relying on non-semantic features such as text length. After this, we need to explain the limitations. Abstracts, introductions, methods sections, and other parts of scientific papers may differ in length. If length is not controlled, the classifier may learn length-based patterns. It is also worth talking about section-specific vocabulary. Macro sections may contain phrases such as “we propose,” “we introduce,” “our contribution,” or “the purpose of this work.” Micro sections may contain words such as “table,” “accuracy,” “dropout,” “training,” “experiment,” and numerical values. Another risk is domain leakage, because if the model is trained and tested only on one domain, it may learn domain-specific writing patterns. Therefore, cross-domain evaluation helps test whether the learned signal transfers to another field. It is also important that the scientific papers and documents used for training should not leak into the test dataset. Text spans from the same paper should not appear in both training and testing. Otherwise, the classifier may simply learn the vocabulary, topic, or writing style of a particular paper rather than learning a more general SLoD signal. Therefore, the dataset should be split by paper ID, not just randomly by text span.
```

## Major prompt 2 — working prototype (verbatim)

```text
Now I need you to build a working prototype for my research task called slot probing. And the goal is to test whether semantic level of detail is linearly decodable from frozen scientific text embeddings. And the pipeline should generate weak labels from scientific document structure, then extract frozen embeddings out of it. After that, train a linear probing classifier, and then evaluate whether the classifier can predict macro, meso, and micro text spans. Please use the project structure that I will attach below. And the core idea that we will load structured scientific papers from Hugging Face, use the QASPER by default for that, and after that extract text spans from each paper, and assign weak slot labels based on the document structure in the domain NLP and computer science, two different domains. For weak labeling rules, we have macro section that includes such criteria as, such sections as title, abstract, first two paragraphs of introduction, conclusion. Then for meso, it is section lead paragraphs of first sentence of each non-introduction and non-conclusion section. And for micro, it can be methods, experiments, results, evaluation, implementation, approach, or detailed results. And please write that weak supervision in this case, it is rule-based labels generated from section structure. And save each extracted span with metadata, paper ID, domain, then section name, paragraph index, label, text, and token count. And save the extracted spans accordingly to the structure of the project. And we have a few extraction control rules that we need to collect at least 500 spans per class, macro, meso, micro. Then we need to use at least 50 papers. The balance class sizes should be within 10%. We need to avoid letting one paper dominate the dataset. Add a configurable max spans per paper per label. And use broad token filtering for the normal dataset. For example, minimum tokens of 30 and max tokens 300. And then save token count for every span. And now this first part is not the length control experiment, it's only for code filtering. And then we need to split data by paper ID. No text spans from the same paper must not appear in both train and test. Use something like 70% papers train, 10% papers validation, and 20% papers test. Save split information so results will be reproducible in this case. And then extract frozen embeddings for each span. Use the model by default, the name of the model I will attach below as well. Do not fine-tune the model because it should remain unchanged. Then train a linear probing classifier. Use logistic regression by default. And then implement three experiment conditions. The first one is within one domain. So it will be training and testing on the same domain, but on different papers. The second one is cross-domain, training on one domain and test on another domain. And length controlled. So we need to create a separate length controlled version of the dataset. Do not replace the initial one. For evaluation metrics, report for each condition: accuracy, macro-averaged F1, per-class precision, per-class recall, confusion matrix, majority class baseline. The majority class baseline should not use embeddings. It just need to predict the most frequent class in the training labels. Then add CLI commands.
model: allenai/scibert_scivocab_uncased
Representation: Mean-pooled token embeddings (or [CLS] token)
No fine-tuning: weights must remain frozen throughout
Dataset:  QASPER (allenai/qasper). Extract at least 500 text spansper label class across 50+ papers from 1-2 domains  NLP and Computer Vision
Requirements once again: 
Document which section each span came from
Balance class sizes to within 10% of each other
Save spans with metadata: paper_id, section_name, label, text, token_count
I would also like to add a few automated diagnostic checks. please add a section-name-only baseline where the model predicts macro, meso, or micro using only section_name, without using the actual text or embeddings. This baseline should use the same paper-level train/test split as the main experiment and report the same metrics: accuracy, macro-F1, per-class precision/recall, and confusion matrix. The goal is to compare this baseline with the embedding probe and understand whether embeddings add signal beyond section names.

Also, during weak label generation, please save more metadata about how each label was created. In addition to the final label, add a column such as label_source_rule, for example macro_abstract, macro_intro_first2, macro_conclusion, meso_section_lead, micro_methods_nonlead, or micro_results_nonlead. Also add a broader section_family column, such as abstract, introduction, conclusion, methods, results, evaluation, or other. After extraction, generate a small automatic summary showing how many examples came from each rule and each section family, how many spans were dropped because no rule applied, and how many papers contributed to each class. Save this summary in the results/ folder.

Code Organization:
slod-probe/
├── README.md                  # Setup, usage, dataset download instructions
├── requirements.txt
├── src/
│   ├── dataset.py             # S2ORC/QASPER loading and weak label assignment
│   ├── embed.py               # Frozen embedding extraction
│   ├── probe.py               # Linear classifier training and evaluation
│   ├── controls.py            # Length-matching and confound analysis
│   └── utils.py
├── data/
│   └── spans/                 # Extracted spans with labels (CSV or JSONL)
├── embeddings/                # Cached embeddings (numpy .npy files)
├── results/                   # Evaluation outputs, confusion matrices
└── notebooks/
   └── analysis.ipynb         # Optional: visualization of embedding space
GitHub Repository
Working prototype runnable with: python src/probe.py --train --eval --condition in_domain
Cached embeddings or clear instructions to reproduce them
Results directory with saved metrics


do you need any additional information?
```

## Major prompt 3 — technical report (verbatim)

```text
Can you please structure a technical report, without adding anything from yourself, and using only what I am going to say now, placing the information into the appropriate parts of the report? So, the introduction should answer the questions: what is SLoD and why does it matter for TKH? We can say that SLoD is Semantic Level of Detail, which describes how abstract or detailed a text span is. In other words, it is the level of abstraction of a particular text excerpt. In this experiment, we differentiate three levels of detail: macro, which means broad motivation, contributions, summaries, and conclusions; meso, which means section-level framing or review; and micro, which means methodological implementation, experimental details, or numerical details. SLoD matters for TKH because it can help organize extracted knowledge by granularity. It can give a retrieval routing signal toward the appropriate level of detail. Our main research question is whether SLoD is linearly decodable from frozen scientific text embeddings. In other words, can a simple logistic regression classifier distinguish macro, meso, and micro spans from frozen SciBERT embeddings under controlled conditions? In the methodology section, explain that in our case we used weak supervision, meaning a weak label generation strategy where labels were generated automatically from scientific document structure instead of manual annotation. We had particular label rules: for macro, abstracts, the first two introduction paragraphs, and conclusion paragraphs; for meso, the first sentence of the lead paragraph of eligible sections; and for micro, non-lead paragraphs in method, approach, implementation, experiments, results, and evaluation sections. For each extracted span, we had the following fields: paper ID, domain, section name, section family, paragraph index, label, label source rule, text, token count, and row ID. We primarily used the QASPER dataset from Hugging Face, with NLP as the domain. In the final dataset, we had 5,292 spans, 877 papers, and 1,764 spans per class, with exact class balance. All spans were filtered to 32–300 whitespace tokens. We divided papers by ID and had 613 training papers, 87 validation papers, and 177 test papers, and no paper appeared in more than one split. For the external cross-domain test, we used the NCBI-PMC Open Access BioC API in the biomedical domain, with a biomedical test set of 1,500 spans from 197 papers, exactly 500 spans per class, and 30–300 tokens per span. We used the AllenAI SciBERT uncased model because it was pretrained on scientific language. The encoder remained completely frozen during the experiment, with attention-mask-aware mean pooling. We also had a linear probe, logistic regression with balanced class weights, so that it could not learn complex nonlinear patterns from the embeddings. We tested it against a majority baseline that always predicted the most frequent training class and did not use embeddings or text, and we also tested it against a section-name baseline, which used only the section name. We tested three experimental conditions: in-domain, length-controlled, and cross-domain biomedical. In the results section, please create all the necessary tables based on the results: an overall performance table with condition and model columns, then accuracy and macro F1 columns; in-domain results with class, precision, recall, and F1; the in-domain confusion matrix; length-controlled results; the length-controlled confusion matrix; cross-domain biomedical results; and the cross-domain biomedical confusion matrix. The interpretation should be that the probe substantially outperformed both baselines, which suggests that frozen embeddings contain predictive information beyond section names in the normal in-domain condition. In the length-controlled condition, there was a decrease in macro F1, which demonstrates that span length was a meaningful confound, but the result remained substantially above the majority baseline. For the cross-domain biomedical results, we can see that the probe remained far above the cross-domain majority baseline. However, in all cases, the embedding probe was approximately tied with the section-name baseline. So from these results we can say that weak SLoD labels are linearly decodable, length explained a significant portion of performance, and section names remained a very strong predictive signal. For the error analysis section, explain that the most difficult class appeared to be micro. The micro F1 was 0.856 in-domain, 0.739 under length control, and 0.765 in the cross-domain biomedical condition. This can be explained by the fact that micro spans are diverse and can contain methodological aspects, implementation details, numerical results, interpretations, and broad summaries as well. Therefore, micro is less linguistically consistent than the other classes. Meso was not the hardest class, since the results showed that it is frequently recognizable by some expressions like “in this section, we…”, “we first describe…”, “the following experiment…”, and so on. So there may be rhetorical cues in it, not only abstraction. The biggest confusion was between macro and micro. In-domain, the largest error was that micro text appeared in the results as macro, with 38 such examples. Under length control, macro was classified as micro in 63 cases, and micro was classified as macro in 60 cases. In the biomedical cross-domain condition, macro was classified as micro in 76 cases, and micro was classified as macro in 76 cases as well. A possible explanation could be that results sections sometimes summarize findings in a broad way, and conclusions may contain detailed descriptions or technical aspects of a paper. So this can result in a situation where a technically detailed conclusion receives a macro weak label because of its section location, but is predicted as micro. Some of this can be caused by weak label noise, because the labeling rules can have errors since the data was not manually annotated. Also mention that I created t-SNE visualizations where we can see partial clustering by SLoD class, and insert the t-SNE graph into the technical report. If there are any additional visualizations that are already available and relevant, include them as well, but do not invent new results. For the discussion and future work section, answer the question: is SLoD linearly decodable from frozen embeddings? Yes, it is decodable, but at the same time in this experiment we could not fully eliminate some confounds, such as the section-name confound. We saw that embeddings could predict the labels, but we also saw a strong correlation with section names, and this may be connected to the language and structure of scientific papers. So I would formulate the conclusion carefully: SLoD is linearly decodable from frozen scientific text embeddings under these experimental conditions, but the signal may partly depend on the structure of scientific papers. Then answer how SLoD could be used as a routing signal in a RAG system. It can help the model decide at what level of detail it should answer. If the user forms a query that requires macro-level information, then the RAG system can retrieve macro-level spans and generate a macro-level answer. Then the user will be more satisfied because they receive the type of information they actually need. If the user needs technical or numerical details, the system can retrieve micro-level spans instead. For additional experiments that would strengthen the findings, say that first it would be important to obtain human-annotated SLoD labels to evaluate our results more precisely and to distinguish true abstraction level from structural patterns. Then we could compare SciBERT with general BERT and other models. Then we could test on non-scientific documents to measure dependence on scientific writing conventions. Then we could investigate continuous SLoD regression instead of three discrete classes. It would also be very important to run experiments that help determine whether this is a general rule, meaning whether SLoD is linearly decodable from frozen embeddings in general, or whether it really depends on the structure of papers, so that it works for scientific papers but may not work for other general text formats. Please keep the technical report concise, honest, and research-oriented. Do not overclaim that the model understands SLoD. The correct interpretation is that weak SLoD labels are linearly decodable from frozen embeddings under the tested conditions, while length and section structure remain important confounds.
```

## What was accepted or modified

AI-assisted code and prose were reviewed and modified where necessary. Important project decisions included treating structural labels as weak proxies, splitting data by paper ID, using frozen SciBERT embeddings with attention-mask-aware mean pooling, using a separate fixed-length control dataset, and initially reporting cross-domain evaluation as unavailable rather than fabricating a QASPER domain. A later extension added a separately sourced PMC BioC biomedical external test set and explicitly treated the result as combined domain-and-corpus transfer.

## Verification performed

- Checked the extracted-data schema, class counts, paper counts, token lengths, and paper-level split disjointness.
- Re-ran the in-domain and length-controlled probes from cached artifacts.
- Verified metric files, confusion matrices, qualitative examples, and t-SNE artifacts.
- Collected and audited a balanced PMC BioC external test set, verified its manifest and per-paper caps, and evaluated the unchanged QASPER-trained probe without biomedical adaptation.
- Ran the automated test suite and source-coverage check.
- Reviewed the README, CLI commands, cached embeddings, limitations, and GitHub contents.
- Kept the technical-report draft outside the public GitHub repository for manual rewriting.

AI-generated suggestions were not treated as experimental evidence. Claims in the repository are based on saved artifacts and rerun checks.
